# EngLearn 配置文件
# 环境变量优先级高于此文件

llm:
  provider: custom          # openai / ollama / custom
  model: deepseek-v4-pro      # 模型名称
  api_key: sk-4e21ebdbc24a481dbd4a9975756a5494           # API Key (建议通过环境变量 LLM_API_KEY 设置)
  base_url: https://api.deepseek.com          # 自定义 endpoint (Ollama 默认 http://localhost:11434)
  temperature: 0.7
  max_tokens: 2000

study:
  daily_word_count: 20    # 每日新学单词数
